<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="description" content="AI Creative Suite - Experiência imersiva de criação com IA generativa. Design futurista, personalização inteligente e multimodalidade total." />
    <meta name="theme-color" content="#0a0a0a" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
    <title>AI Creative Suite ✨ Studio Criativo com IA</title>

    <!-- Premium Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Sora:wght@300;400;600;700;800&family=Space+Grotesk:wght@400;500;700&family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* ========================================
           🎨 DESIGN SYSTEM - AI CREATIVE SUITE 2025
           World-class UI with cutting-edge trends
           ======================================== */

        :root {
            /* Core Brand Colors - Vibrant & Futuristic */
            --bg-void: #000000;
            --bg-deep: #0a0a0f;
            --bg-charcoal: #12121c;
            --bg-midnight: #1a1a2e;

            /* Glass & Frosted Effects */
            --glass-light: rgba(255, 255, 255, 0.03);
            --glass-medium: rgba(255, 255, 255, 0.08);
            --glass-strong: rgba(255, 255, 255, 0.12);
            --glass-border: rgba(255, 255, 255, 0.1);

            /* Vibrant Gradient Palette */
            --gradient-aurora: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #4facfe 75%, #00f2fe 100%);
            --gradient-sunset: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
            --gradient-ocean: linear-gradient(135deg, #2afadf 0%, #4c83ff 100%);
            --gradient-fire: linear-gradient(135deg, #ff6b6b 0%, #ffa07a 50%, #ffed4e 100%);
            --gradient-neon: linear-gradient(135deg, #00ff87 0%, #00d9ff 50%, #667eea 100%);
            --gradient-cosmic: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #fa709a 100%);

            /* Metallic & Shader Effects */
            --metallic-silver: linear-gradient(135deg, #b8b8b8 0%, #e8e8e8 50%, #b8b8b8 100%);
            --metallic-gold: linear-gradient(135deg, #d4af37 0%, #f4e6a8 50%, #d4af37 100%);
            --metallic-copper: linear-gradient(135deg, #b87333 0%, #e5a56c 50%, #b87333 100%);

            /* Core UI Colors */
            --indigo-500: #6366f1;
            --indigo-600: #4f46e5;
            --purple-500: #a855f7;
            --pink-500: #ec4899;
            --rose-500: #f43f5e;
            --cyan-400: #22d3ee;
            --teal-400: #2dd4bf;
            --green-400: #4ade80;
            --amber-400: #fbbf24;

            /* Typography */
            --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            --font-display: 'Sora', sans-serif;
            --font-mono: 'Space Grotesk', monospace;
            --font-organic: 'DM Sans', sans-serif;

            /* Spacing & Rhythm */
            --spacing-xs: 0.5rem;
            --spacing-sm: 0.75rem;
            --spacing-md: 1rem;
            --spacing-lg: 1.5rem;
            --spacing-xl: 2rem;
            --spacing-2xl: 3rem;

            /* Border Radius - Organic Curves */
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 16px;
            --radius-xl: 24px;
            --radius-2xl: 32px;
            --radius-full: 9999px;

            /* Shadows - Depth & Elevation */
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.1);
            --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.15);
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.2);
            --shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.25);
            --shadow-glow: 0 0 32px rgba(102, 126, 234, 0.4);
            --shadow-neon: 0 0 20px rgba(34, 211, 238, 0.6);

            /* Animation Timing */
            --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
            --ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
            --ease-elastic: cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        /* ========================================
           ☀️ LIGHT THEME - Clean & Bright
           ======================================== */

        [data-theme="light"] {
            /* Core Background Colors - Light & Airy */
            --bg-void: #ffffff;
            --bg-deep: #f8f9fa;
            --bg-charcoal: #f0f1f4;
            --bg-midnight: #e8eaed;

            /* Glass & Frosted Effects - Inverted */
            --glass-light: rgba(0, 0, 0, 0.02);
            --glass-medium: rgba(0, 0, 0, 0.05);
            --glass-strong: rgba(0, 0, 0, 0.08);
            --glass-border: rgba(0, 0, 0, 0.1);

            /* Shadows - Softer for Light Mode */
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.06);
            --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.08);
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.1);
            --shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.12);
            --shadow-glow: 0 0 32px rgba(102, 126, 234, 0.2);
            --shadow-neon: 0 0 20px rgba(34, 211, 238, 0.3);
        }

        [data-theme="light"] body {
            background: linear-gradient(to bottom, #ffffff 0%, #f8f9fa 100%);
            color: #1a1a2e;
        }

        [data-theme="light"] body::before {
            background:
                radial-gradient(ellipse at 20% 30%, rgba(102, 126, 234, 0.08) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 70%, rgba(236, 72, 153, 0.06) 0%, transparent 50%),
                radial-gradient(ellipse at 50% 50%, rgba(34, 211, 238, 0.04) 0%, transparent 50%);
            opacity: 1;
        }

        [data-theme="light"] body::after {
            background-image:
                radial-gradient(2px 2px at 20% 30%, rgba(102, 126, 234, 0.2), transparent),
                radial-gradient(2px 2px at 60% 70%, rgba(236, 72, 153, 0.15), transparent),
                radial-gradient(1px 1px at 50% 50%, rgba(34, 211, 238, 0.1), transparent),
                radial-gradient(1px 1px at 80% 10%, rgba(168, 85, 247, 0.1), transparent);
            opacity: 0.4;
        }

        [data-theme="light"] .glass-morphism {
            background: rgba(255, 255, 255, 0.6);
            border: 1px solid rgba(0, 0, 0, 0.1);
            box-shadow: var(--shadow-lg), inset 0 1px 0 rgba(255, 255, 255, 0.8);
        }

        [data-theme="light"] .glass-card {
            background: rgba(255, 255, 255, 0.7);
            border: 1px solid rgba(0, 0, 0, 0.06);
            box-shadow: var(--shadow-md), inset 0 1px 0 rgba(255, 255, 255, 0.9);
        }

        [data-theme="light"] .glass-card:hover {
            background: rgba(255, 255, 255, 0.85);
            border-color: rgba(102, 126, 234, 0.2);
            box-shadow: var(--shadow-xl), 0 0 24px rgba(102, 126, 234, 0.15);
        }

        [data-theme="light"] .neuro-card {
            background: linear-gradient(145deg, #ffffff, #f0f1f4);
            box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.08),
                       -8px -8px 16px rgba(255, 255, 255, 0.9);
        }

        [data-theme="light"] .neuro-btn {
            background: linear-gradient(145deg, #f8f9fa, #e8eaed);
            box-shadow: 6px 6px 12px rgba(0, 0, 0, 0.06),
                       -6px -6px 12px rgba(255, 255, 255, 0.8);
        }

        [data-theme="light"] ::-webkit-scrollbar-track {
            background: #f0f1f4;
        }

        [data-theme="light"] ::-webkit-scrollbar-thumb {
            background: var(--gradient-cosmic);
            border-color: #f0f1f4;
        }

        /* Global Reset & Smooth Experience */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            overflow-x: hidden;
        }

        body {
            font-family: var(--font-sans);
            background: var(--bg-void);
            color: #e2e8f0;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            text-rendering: optimizeLegibility;
            overflow-x: hidden;
            position: relative;
        }

        /* Immersive Animated Background */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background:
                radial-gradient(ellipse at 20% 30%, rgba(102, 126, 234, 0.15) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 70%, rgba(236, 72, 153, 0.12) 0%, transparent 50%),
                radial-gradient(ellipse at 50% 50%, rgba(34, 211, 238, 0.08) 0%, transparent 50%);
            animation: breathe 15s ease-in-out infinite;
            z-index: 0;
            pointer-events: none;
        }

        @keyframes breathe {
            0%, 100% { opacity: 0.6; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.05); }
        }

        /* Floating Particles Effect */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image:
                radial-gradient(2px 2px at 20% 30%, rgba(255, 255, 255, 0.3), transparent),
                radial-gradient(2px 2px at 60% 70%, rgba(255, 255, 255, 0.3), transparent),
                radial-gradient(1px 1px at 50% 50%, rgba(255, 255, 255, 0.2), transparent),
                radial-gradient(1px 1px at 80% 10%, rgba(255, 255, 255, 0.2), transparent);
            background-size: 200px 200px, 300px 300px, 400px 400px, 250px 250px;
            animation: float 60s linear infinite;
            z-index: 0;
            opacity: 0.3;
            pointer-events: none;
        }

        @keyframes float {
            from { transform: translate(0, 0); }
            to { transform: translate(100px, -100px); }
        }

        /* Content Layer Above Background */
        #root {
            position: relative;
            z-index: 1;
        }

        /* Premium Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
            height: 10px;
        }
        ::-webkit-scrollbar-track {
            background: var(--bg-charcoal);
            border-radius: var(--radius-sm);
        }
        ::-webkit-scrollbar-thumb {
            background: var(--gradient-cosmic);
            border-radius: var(--radius-full);
            border: 2px solid var(--bg-charcoal);
        }
        ::-webkit-scrollbar-thumb:hover {
            background: var(--gradient-aurora);
        }

        /* Font Families */
        .font-sora { font-family: var(--font-display); }
        .font-mono { font-family: var(--font-mono); }
        .font-organic { font-family: var(--font-organic); }

        /* ========================================
           🔮 GLASSMORPHISM - Ultra Premium
           ======================================== */

        .glass-morphism {
            background: rgba(26, 26, 46, 0.4);
            backdrop-filter: blur(24px) saturate(180%);
            -webkit-backdrop-filter: blur(24px) saturate(180%);
            border: 1px solid var(--glass-border);
            box-shadow: var(--shadow-lg),
                        inset 0 1px 0 rgba(255, 255, 255, 0.1);
        }

        .glass-card {
            background: rgba(18, 18, 28, 0.5);
            backdrop-filter: blur(20px) saturate(160%);
            -webkit-backdrop-filter: blur(20px) saturate(160%);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: var(--radius-xl);
            box-shadow: var(--shadow-md),
                        inset 0 1px 0 rgba(255, 255, 255, 0.05);
            transition: all 0.4s var(--ease-smooth);
        }

        .glass-card:hover {
            background: rgba(18, 18, 28, 0.6);
            border-color: rgba(255, 255, 255, 0.15);
            box-shadow: var(--shadow-xl), var(--shadow-glow);
            transform: translateY(-2px);
        }

        /* ========================================
           🎭 NEUMORPHISM - Soft Depth
           ======================================== */

        .neuro-card {
            background: linear-gradient(145deg, #14141f, #10101a);
            box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.4),
                       -8px -8px 16px rgba(30, 30, 45, 0.3);
            border-radius: var(--radius-xl);
            transition: all 0.3s var(--ease-smooth);
        }

        .neuro-card:active {
            box-shadow: inset 4px 4px 8px rgba(0, 0, 0, 0.4),
                        inset -4px -4px 8px rgba(30, 30, 45, 0.3);
        }

        .neuro-btn {
            background: linear-gradient(145deg, #1a1a2e, #12121c);
            box-shadow: 6px 6px 12px rgba(0, 0, 0, 0.3),
                       -6px -6px 12px rgba(30, 30, 45, 0.2);
            border: none;
            transition: all 0.2s var(--ease-smooth);
        }

        .neuro-btn:hover {
            box-shadow: 8px 8px 16px rgba(0, 0, 0, 0.4),
                       -8px -8px 16px rgba(30, 30, 45, 0.3);
        }

        .neuro-btn:active {
            box-shadow: inset 3px 3px 6px rgba(0, 0, 0, 0.4),
                        inset -3px -3px 6px rgba(30, 30, 45, 0.3);
        }

        /* ========================================
           ✨ BUTTONS - Hyper Interactive
           ======================================== */

        .btn-primary {
            position: relative;
            background: var(--gradient-cosmic);
            background-size: 200% 200%;
            color: white;
            font-weight: 600;
            border: none;
            border-radius: var(--radius-lg);
            padding: 1rem 2rem;
            cursor: pointer;
            overflow: hidden;
            box-shadow: var(--shadow-lg), 0 0 20px rgba(102, 126, 234, 0.3);
            transition: all 0.4s var(--ease-smooth);
            animation: gradientShift 3s ease infinite;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-primary:hover {
            transform: translateY(-2px) scale(1.02);
            box-shadow: var(--shadow-xl), 0 0 40px rgba(102, 126, 234, 0.5);
        }

        .btn-primary:hover::before {
            width: 400px;
            height: 400px;
        }

        .btn-primary:active {
            transform: translateY(0) scale(0.98);
        }

        .btn-primary:disabled {
            background: linear-gradient(135deg, #2c2c44 0%, #1a1a2e 100%);
            cursor: not-allowed;
            opacity: 0.5;
            box-shadow: none;
            animation: none;
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            font-weight: 500;
            border-radius: var(--radius-lg);
            padding: 0.875rem 1.75rem;
            cursor: pointer;
            transition: all 0.3s var(--ease-smooth);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-1px);
        }

        /* ========================================
           🎬 ANIMATIONS - Cinematic & Smooth
           ======================================== */

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px) scale(0.98);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes bounceIn {
            0% {
                opacity: 0;
                transform: scale(0.3) translateY(-50px);
            }
            50% {
                opacity: 1;
                transform: scale(1.05);
            }
            70% {
                transform: scale(0.95);
            }
            100% {
                transform: scale(1) translateY(0);
            }
        }

        @keyframes shimmer {
            0% { background-position: -1000px 0; }
            100% { background-position: 1000px 0; }
        }

        @keyframes pulse {
            0%, 100% {
                opacity: 1;
                transform: scale(1);
            }
            50% {
                opacity: 0.8;
                transform: scale(1.05);
            }
        }

        @keyframes glow {
            0%, 100% {
                box-shadow: 0 0 20px rgba(102, 126, 234, 0.3);
            }
            50% {
                box-shadow: 0 0 40px rgba(102, 126, 234, 0.6);
            }
        }

        .animate-fade-in {
            animation: fadeIn 0.6s var(--ease-smooth) forwards;
        }

        .animate-fade-in-scale {
            animation: fadeInScale 0.5s var(--ease-elastic) forwards;
        }

        .animate-slide-in-left {
            animation: slideInLeft 0.5s var(--ease-smooth) forwards;
        }

        .animate-slide-in-right {
            animation: slideInRight 0.5s var(--ease-smooth) forwards;
        }

        .animate-bounce-in {
            animation: bounceIn 0.8s var(--ease-bounce) forwards;
        }

        .animate-pulse {
            animation: pulse 2s ease-in-out infinite;
        }

        .animate-glow {
            animation: glow 2s ease-in-out infinite;
        }

        /* ========================================
           🌈 GRADIENT TEXT - Vibrant Typography
           ======================================== */

        .text-gradient {
            background: var(--gradient-cosmic);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
        }

        .text-gradient-aurora {
            background: var(--gradient-aurora);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .text-gradient-neon {
            background: var(--gradient-neon);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .text-shimmer {
            background: linear-gradient(90deg,
                rgba(255, 255, 255, 0.4) 0%,
                rgba(255, 255, 255, 0.9) 50%,
                rgba(255, 255, 255, 0.4) 100%);
            background-size: 1000px 100%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: shimmer 3s linear infinite;
        }

        /* ========================================
           🎨 UTILITY CLASSES - Design Tokens
           ======================================== */

        .blur-effect {
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
        }

        .border-gradient {
            border: 2px solid transparent;
            background-clip: padding-box;
            position: relative;
        }

        .border-gradient::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-cosmic);
            border-radius: inherit;
            z-index: -1;
        }

        .hover-lift {
            transition: transform 0.3s var(--ease-smooth), box-shadow 0.3s var(--ease-smooth);
        }

        .hover-lift:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-xl);
        }

        .hover-scale {
            transition: transform 0.3s var(--ease-smooth);
        }

        .hover-scale:hover {
            transform: scale(1.05);
        }

        .interactive-3d {
            transform-style: preserve-3d;
            transition: transform 0.3s var(--ease-smooth);
        }

        .interactive-3d:hover {
            transform: perspective(1000px) rotateY(5deg) rotateX(5deg);
        }

        /* ========================================
           📱 RESPONSIVE & ACCESSIBILITY
           ======================================== */

        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        @media (max-width: 768px) {
            :root {
                --spacing-xl: 1.5rem;
                --spacing-2xl: 2rem;
            }

            .glass-card {
                backdrop-filter: blur(16px);
            }

            .btn-primary {
                padding: 0.875rem 1.5rem;
            }
        }

        /* Focus States for Accessibility */
        *:focus-visible {
            outline: 2px solid var(--indigo-500);
            outline-offset: 2px;
            border-radius: var(--radius-sm);
        }

        /* Custom Prose Styles */
        .prose {
            max-width: none;
            color: #e2e8f0;
        }

        .prose h1, .prose h2, .prose h3 {
            color: white;
            font-family: var(--font-display);
        }

        .prose strong {
            color: white;
            font-weight: 700;
        }

        .prose code {
            background: rgba(255, 255, 255, 0.1);
            padding: 0.2em 0.4em;
            border-radius: var(--radius-sm);
            font-family: var(--font-mono);
        }

    </style>
    <script type="importmap">
{
  "imports": {
    "react": "https://aistudiocdn.com/react@^19.2.0",
    "react-dom/": "https://aistudiocdn.com/react-dom@^19.2.0/",
    "react/": "https://aistudiocdn.com/react@^19.2.0/",
    "@google/genai": "https://aistudiocdn.com/@google/genai@^1.29.0",
    "marked": "https://aistudiocdn.com/marked@^16.4.1",
    "jszip": "https://aistudiocdn.com/jszip@^3.10.1",
    "react-dropzone": "https://aistudiocdn.com/react-dropzone@^14.3.8"
  }
}
</script>
</head>
<body>
    <div id="root"></div>
    <script type="module" src="/index.tsx"></script>
</body>
</html>